# LastDone 产品与技术设计

日期：2026-09-05  
状态：已在对话中确认；等待书面设计文档复核  
暂定产品名称：LastDone

## 1. 产品目的

LastDone 是一款个人使用、可自行托管的应用，用于管理那些不属于每日待办，
但需要按一定周期重复完成的事情。

例如：更换净水器滤芯、备份电脑、清洗空调滤网、看牙医、保养车辆和检查
家用设备。

这个产品主要回答三个问题：

1. 这件事上次是什么时候完成的？
2. 下一次应该什么时候完成？
3. 哪些事情已经逾期，或者即将到期？

LastDone 不是通用任务管理器、习惯打卡工具、日记或日历，也不提供游戏化
机制。版本 1 不包含连续天数、积分、每日签到、自由笔记、附件、共享列表和
公开注册。

## 2. 产品优先级

各客户端按以下顺序实现：

1. 可从 Safari 添加到 iPhone 主屏幕的 Web 应用。
2. 可直接在电脑浏览器中使用的响应式应用。
3. 可在 Redmi K80 Ultra 上侧载安装的已签名 Android APK。

所有客户端共用同一个账号和同一份同步数据。首次成功登录并同步后，每个
客户端在离线状态下仍然可以使用。

版本 1 只支持单用户。从一开始就在数据模型中加入所有权边界，以便未来增加
家庭成员账号和选择性共享事项时，无需重做存储层。

## 3. 用户体验

### 3.1 首页

首页是应用的默认页面。事项按以下顺序分组：

1. 已逾期
2. 今天到期
3. 即将到期
4. 状态正常
5. 尚未开始

同一分组内，到期日期越近的事项越靠前。首页显示已逾期、今天到期和即将
到期事项的数量。

每张事项卡片显示：

- 分类图标和颜色；
- 事项名称；
- 相对状态，例如“已逾期 12 天”或“还有 7 天”；
- 上次完成日期（如果存在）；
- 一键“完成”操作；
- 用于低频操作的更多菜单。

首页支持文本搜索和按单个分类筛选。搜索范围包括事项名称和完成备注。

“即将到期”默认范围为未来七天，可在设置中修改。

### 3.2 分类

每个事项只能属于一个分类。版本 1 提供以下默认分类：

- 健康
- 家庭
- 数字生活
- 设备
- 车辆
- 其他

一个分类包含名称、图标、颜色和显示顺序。用户可以新增、改名、换色、排序、
归档和恢复分类。重要程度是事项自身的属性，不作为标签使用。

版本 1 明确不加入多标签系统。

### 3.3 新建和编辑事项

一个事项包含：

- 名称；
- 分类；
- 重复类型和重复规则；
- 上次完成日期或首次到期日；
- 是否重要；
- 提前提醒时间；
- 启用、暂停、归档或删除状态。

新建事项时，用户从以下初始条件中选择一种：

- 填写已知的上次完成日期，由系统计算首次到期日；
- 直接填写首次到期日；
- 标记为“从未完成”，并填写首次提醒日期。

保存前，表单会预览计算出的到期日期和计划发送的重要提醒。

### 3.4 完成事项

点击“完成”后立即记录当前时间并更新下一次到期日期，不会强制打开填写表单。

完成后：

- 短时间内显示“撤销”操作；
- 新完成记录出现在事项历史中；
- 相对周期根据本次完成重新计算；
- 固定日历周期推进到下一个日历节点；
- 取消当前到期轮次尚未发送的提醒。

更多菜单提供：

- 完成并添加可选备注；
- 补录过去日期的完成记录；
- 跳过当前固定日历轮次；
- 暂停事项。

每条完成记录保存完成时间和一段可选短备注。备注不是必填项，也不会阻碍
一键完成。

只有固定日历事项可以“跳过”。跳过会生成单独的历史记录，不会伪造成一条
完成记录。相对周期事项不能跳过，因为它的下次到期日必须基于一次真实完成
时间计算。

编辑或删除历史完成记录需要确认，因为此操作可能改变计算出的到期日期。

### 3.5 事项详情

事项详情页显示：

- 当前状态；
- 距离上次完成已经过去多久；
- 下次到期日期；
- 重复规则；
- 提醒规则；
- 完成和跳过历史；
- 每次完成附带的备注；
- 编辑、暂停、归档、恢复和删除操作。

归档是常规移除方式，会保留历史数据。永久删除必须进行第二次确认。

### 3.6 设置

设置内容包括：

- 时区；
- 每日摘要时间；
- 默认“即将到期”范围；
- 免打扰时间；
- 每台设备的通知状态；
- 每台设备的每日摘要和重要提醒开关；
- 设备会话管理；
- 修改密码；
- 同步状态；
- 完整 JSON 导出；
- 事项和完成历史 CSV 导出；
- 恢复或导入预览；
- 应用和服务器版本信息。

默认时区为 Asia/Shanghai。默认免打扰时段为当地时间 22:00 至 08:00。

## 4. 周期规则

版本 1 支持两类周期。

### 4.1 基于完成时间的相对周期

相对周期事项在最近一次完成后的指定时间间隔到期。

支持的单位：

- 天；
- 周；
- 日历月；
- 日历年。

例如每 30 天、每 12 周、每 6 个月或每年一次。

按日历月和日历年计算时，如果目标日期不存在，则使用目标月份的最后一个
有效日期。例如，1 月 31 日的一个月后是 2 月 28 日或 2 月 29 日；闰年
2 月 29 日的一年后，在非闰年中是 2 月 28 日。

提前或延迟完成都会移动下次到期日，因为最近一次完成会成为新的计算基准。

### 4.2 固定日历周期

固定日历周期始终锚定在日历日期上。版本 1 支持：

- 每月的指定日期；
- 每年的指定月份和日期。

如果每月指定的日期在某个较短月份中不存在，则使用该月最后一天。每年
2 月 29 日的事项在非闰年中于 2 月 28 日到期。

提前或延迟完成不会永久移动后续轮次。跳过当前轮次会直接进入下一个固定
轮次，同时不新增完成记录。

### 4.3 状态计算

需要精确时间戳的日期以 UTC 时刻保存。日历轮次按照用户配置的时区计算。

事项状态定义如下：

- 到期日期早于当前本地日期时为“已逾期”；
- 到期日期等于当前本地日期时为“今天到期”；
- 到期日期位于用户配置的即将到期范围内时为“即将到期”；
- 从未完成且首次到期日尚未进入即将到期范围时为“尚未开始”；
- 至少完成过一次且未进入即将到期范围时为“状态正常”；
- 用户明确暂停或归档时为“已暂停”或“已归档”。

时间状态优先于“从未完成”状态。例如，从未完成的事项会随着首次到期日临近
和经过，依次从“尚未开始”变为“即将到期”“今天到期”和“已逾期”。

服务器保存的下次到期值具有权威性。客户端使用相同规则提供离线显示和表单
预览。一组共享的周期测试向量会同时在 TypeScript 客户端实现和 Go 服务器
实现上运行，防止两端得出不同结果。

## 5. 通知

### 5.1 每日摘要

每日摘要在用户配置的本地时间发送，内容包括：

- 已逾期事项数量；
- 今天到期事项数量；
- 即将到期事项数量；
- 若干个优先级最高的事项名称。

当没有已逾期、今天到期或即将到期事项时，不发送摘要。每台启用摘要的设备
在每个本地自然日最多收到一条。打开通知后进入已经筛选好的首页。

### 5.2 重要提醒

标记为重要的事项可以启用以下一个或多个提前提醒：

- 提前 30 天；
- 提前 7 天；
- 提前 3 天；
- 提前 1 天；
- 到期当天。

版本 1 不会在事项逾期后每天发送单独提醒。逾期事项继续出现在每日摘要中。

落入免打扰时段的提醒会延迟到免打扰结束后发送。LastDone 不提供绕过操作
系统通知控制的紧急警报模式。

### 5.3 设备默认设置

每台已登记设备分别保存通知设置。

建议的初始设置为：

- iPhone：启用每日摘要和重要提醒；
- Android：只启用重要提醒；
- 电脑浏览器：默认全部关闭，由用户手动启用。

这些只是首次设置时显示的建议，并不是不可修改的规则。

### 5.4 通知投递

iPhone 主屏幕应用和电脑浏览器使用由 VPS 发送的标准 Web Push。只有在
用户主动点击后才能请求通知权限。

Android 使用根据同步数据在本机安排的本地通知，不依赖 Google Firebase
Cloud Messaging。客户端会在同步后、应用启动时、回到前台时、网络恢复时
以及操作系统允许的后台执行机会中校对通知计划。界面会提示缺少通知权限或
过于严格的电池限制。

每条通知都具有幂等标识，由以下内容组成：

- 用户；
- 事项或摘要日期；
- 到期轮次；
- 提醒类型和提前时间；
- 目标设备。

因此，重试、服务器重启和定时任务重复执行不会产生重复通知。完成、跳过、
暂停、归档或重新安排事项后，旧提醒任务会失效。

在平台支持应用角标时，应用图标显示当前逾期数量。角标只是增强功能，不是
底层通知成功投递的必要条件。

## 6. 客户端架构

### 6.1 Web 应用

主要客户端是使用 React 和 TypeScript 开发、由 Vite 构建的渐进式 Web
应用。iPhone 主屏幕端和电脑浏览器端共用同一套响应式应用。

PWA 包含：

- Web App Manifest；
- 兼容 iPhone 主屏幕的元数据和图标；
- 用于缓存应用外壳的 Service Worker；
- 离线导航；
- 受控的应用更新；
- Web Push 订阅管理。

带内容哈希的静态资源可以长期缓存。Service Worker 文件必须始终重新验证，
让客户端及时发现新版本。

### 6.2 Android 应用

Capacitor 将 Web 应用打包为已签名 Android APK。Android 原生层保持轻量，
只负责：

- 原生本地通知；
- 通知权限检查；
- 应用生命周期集成；
- 在适当位置安全保存设备会话；
- 从通知进入指定页面；
- 报告应用版本。

业务规则和界面继续由共享 Web 应用提供。

### 6.3 本地存储

每个客户端在 IndexedDB 中保存一份完整可用的数据，通过一个小型数据仓库
抽象层访问。本地数据包括：

- 事项和分类；
- 完成记录和跳过记录；
- 离线显示所需的设置；
- 当前设备记录；
- 最近确认的服务器序列号；
- 等待上传的操作；
- 尚未解决的冲突。

所有用户操作都会先提交到本地并更新界面。正常使用不会被网络可用性阻塞。

## 7. 服务器架构

服务器是一个将 PocketBase 作为应用框架嵌入的定制 Go 二进制程序。
PocketBase 提供 SQLite 持久化、身份验证、记录管理、迁移和管理界面。
定制 Go 包提供：

- 同步接口；
- 操作幂等性；
- 字段级冲突检测；
- 周期规则验证；
- Web Push；
- 通知调度；
- 备份策略；
- 健康检查。

构建后的 PWA 由同一进程提供服务，因此只需要一个容器和一个网络端口。

### 7.1 集合与内部表

业务集合：

- `users`：使用用户名和密码验证的应用用户；
- `categories`：用户拥有的分类定义；
- `items`：用户拥有的重复事项和当前派生周期状态；
- `completions`：以追加为主的完成记录；
- `skips`：固定日历轮次的跳过记录；
- `devices`：设备会话和通知能力；
- `user_settings`：时区、摘要、免打扰和默认偏好；
- `push_subscriptions`：浏览器 Web Push 订阅；
- `notification_log`：具备幂等性的投递历史。

同步辅助数据：

- `sync_changes`：单调递增的服务器序列号和变更记录元数据；
- `processed_operations`：已经处理的客户端操作 ID；
- `sync_conflicts`：被拒绝的重叠编辑及其两个版本。

每条业务记录都包含用户所有者。API 授权规则验证经过身份认证的用户，不信任
客户端直接提供的用户 ID。

版本 1 关闭公开注册。唯一的应用用户由管理员创建。未来版本可以增加基于
邀请的用户，而不改变所有权模型。

## 8. 同步

### 8.1 操作流程

每次修改都会：

1. 创建唯一的客户端操作 ID；
2. 立即修改 IndexedDB；
3. 将操作写入本地发件箱；
4. 更新界面；
5. 在网络允许时把操作推送到服务器。

服务器在同一个数据库事务中应用操作并写入同步变更。已经处理过的操作 ID
再次提交时，返回原始结果，不会创建重复数据。

客户端从最近确认的服务器序列号之后拉取变更。使用序列号可以避免时间戳
游标歧义。以下时机会触发同步：

- 应用启动；
- 应用回到前台；
- 网络恢复；
- 本地修改之后；
- 应用保持打开时定期同步；
- 用户手动刷新之后。

界面显示“已同步”“等待同步”“离线”和“错误”状态。

### 8.2 合并与冲突策略

完成记录和跳过记录是相互独立的记录，因此不同设备分别新增的记录都会保留。

事项和分类的编辑以字段补丁和基础修订版本提交：

- 修改不同字段时自动合并；
- 修改服务器上未发生变化的字段时正常应用；
- 同时修改同一字段时产生冲突。

同字段冲突保存在服务器中，并在界面中展示两个值。任何一方都不会被静默
丢弃。用户选择最终结果后，生成一个新的同步修订版本。

归档和删除作为状态变更同步。永久删除会保留一个最小同步墓碑，直到已登记
设备确认删除；随后移除被删除的完整数据。

### 8.3 身份验证与离线会话

首次登录需要网络连接。应用账号使用用户名和密码，不需要邮件投递能力。

成功登录后：

- 每台设备获得独立、可撤销的会话；
- 缓存数据在离线状态下仍可读取；
- 服务器会话过期时，离线修改继续保留在队列中；
- 重新验证身份后恢复同步，不清空本地数据。

版本 1 通过不公开的 PocketBase 管理界面重置密码。

## 9. 导入、导出与备份

用户可以导出：

- 一份带格式版本、可用于恢复的完整 JSON 归档；
- 事项、完成记录和跳过记录的 CSV 文件。

导入操作始终先验证并预览变更，然后才应用，不会静默替换当前数据集。

服务器创建：

- 每天一份 PocketBase 数据备份，保留最近七份；
- 应用版本变化时创建一份升级前备份，保留最近三份。

备份位于持久化数据目录中，未来可以复制到兼容 S3 的服务。版本 1 不支持
大型附件，因此预期备份增长很小。

## 10. 仓库结构

计划采用以下仓库结构：

    lastdone/
    ├── source/
    │   ├── apps/web/
    │   │   └── android/
    │   ├── server/
    │   └── packages/
    ├── deploy/
    │   └── lastdone.env.example
    ├── docs/
    │   ├── en/
    │   └── zh-CN/
    ├── compose.yml
    ├── Dockerfile
    └── .github/workflows/

`contracts` 包含与具体技术无关的 JSON 测试向量，用于验证日期、周期、状态
和冲突行为。客户端和服务器测试套件都必须通过这些测试向量。

## 11. Docker 镜像

Docker 镜像使用三个构建阶段：

1. Node.js 构建 PWA。
2. Go 编译定制 PocketBase 服务器。
3. 最小运行镜像只接收二进制程序、静态资源、CA 证书和时区数据。

运行镜像不包含 Node.js、npm、构建工具、源代码或签名密钥。进程使用非
root 用户运行并开放 8090 端口。

持久数据挂载到 `/pb/pb_data`。密钥在运行时传入，不写入镜像。Web Push
密钥对在首次启动时生成并保存到持久化数据中。

应用提供内置健康检查命令，因此运行镜像的容器健康检查不依赖 curl 或 wget。

具有代表性的 Compose 配置如下：

    services:
      lastdone:
        image: docker.io/OWNER/lastdone:latest
        container_name: lastdone
        restart: unless-stopped
        ports:
          - "127.0.0.1:8090:8090"
        volumes:
          - lastdone_data:/pb/pb_data
        env_file:
          - .env
        environment:
          TZ: Asia/Shanghai
          GOMEMLIMIT: 384MiB
        healthcheck:
          test: ["CMD", "/usr/local/bin/lastdone", "healthcheck"]
          interval: 30s
          timeout: 5s
          retries: 3

`OWNER` 是创建公开仓库时选定的 Docker Hub 命名空间。

## 12. 生产部署

目标生产访问链路为：

    客户端
       |
       | HTTPS
       v
    Cloudflare
       |
       | 使用 Full (strict) 的 HTTPS
       v
    1Panel / OpenResty
       |
       | 回环地址上的 HTTP
       v
    127.0.0.1:8090 / LastDone 容器
       |
       v
    持久化命名卷中的 /pb/pb_data

容器端口永远不绑定到公网接口。

仓库有意不安装或管理 OpenResty 配置。所有者在现有 1Panel 网站中把代理目标
设置为 `127.0.0.1:8090`，并在 1Panel 和 Cloudflare 界面中自行管理 HTTPS、
Cloudflare 策略及 PocketBase 管理路径的访问方式。管理员密码不得保存在
Compose 或应用环境变量中。

## 13. 持续集成与发布

Pull Request 和普通提交运行：

- TypeScript 格式检查、Lint、类型检查和单元测试；
- Go 格式检查、静态分析和单元测试；
- TypeScript 和 Go 共同运行共享周期契约测试；
- 同步幂等性和冲突测试；
- 生产 PWA 构建；
- Docker 镜像构建；
- 真实容器健康检查；
- 针对运行中容器的浏览器端到端测试。

发布标签使用语义化版本。`v1.2.0` 标签会生成：

- `linux/amd64` Docker 镜像；
- `linux/arm64` Docker 镜像；
- Docker Hub 标签 `latest`、`1`、`1.2`、`1.2.0` 和提交 SHA；
- 已签名的发布版 APK；
- GitHub Release；
- Compose 和环境变量示例文件；
- SHA-256 校验文件；
- 发布说明。

Android 签名密钥加密保存在 GitHub Actions Secrets 中。APK 永远不包含
账号凭据、服务器私钥、Cloudflare 凭据或管理员凭据。

VPS 不会被静默自动更新。所有者通过 1Panel 或 Docker Compose 自主决定
何时拉取并重启稳定版镜像。

## 14. 升级与恢复

容器以新应用版本启动时：

1. 服务器检测版本变化；
2. 在数据库迁移前创建升级前备份；
3. 执行尚未应用的版本化迁移；
4. 启动 HTTP 服务；
5. Docker 执行健康检查。

如果启动或健康检查失败，不允许异常版本无限期替代已知正常的公开服务。
恢复时固定到之前的 Docker 标签；如果数据库结构已经改变，则恢复对应的
升级前备份。

应用版本和数据库版本会显示在设置页面和健康检查输出中。

## 15. 错误处理

面向用户的错误使用可恢复状态表达：

- 离线：接受本地编辑并标记为等待同步；
- 身份验证过期：保留本地数据和待上传修改，重新登录后恢复同步；
- 同步被拒绝：保留操作，可重试或进入冲突处理；
- 通知权限被拒绝：设备仍保持登记，但不投递通知，并显示设置说明；
- 服务器不可用：使用指数退避重试，同时提供手动重试操作；
- 迁移失败：服务器不会在数据库只完成部分迁移的情况下继续运行；
- 导入无效：不提交任何记录，并指出受影响条目的验证错误。

任何常规网络错误或身份验证错误都不会自动清空 IndexedDB。

## 16. 测试策略

关键单元测试范围包括：

- 相对周期和固定周期计算；
- 月末与闰年处理；
- 提前、延迟、补录、撤销、编辑和删除完成记录；
- 固定轮次跳过；
- 午夜附近和时区变化时的状态计算；
- 通知幂等性和免打扰延迟；
- 发件箱重试和操作重放；
- 字段级合并和冲突创建；
- 归档、恢复、删除墓碑、导出和导入验证。

集成测试使用真实的临时 PocketBase 数据库。端到端测试覆盖：

- 首次登录；
- 根据上次完成日期创建事项；
- 根据首次到期日创建事项；
- 离线完成后恢复同步；
- 两个模拟设备同时编辑；
- 一个客户端上的完成记录出现在另一个客户端；
- PWA 安装元数据；
- Docker 使用持久化卷启动；
- 数据库迁移和备份行为。

手动发布检查覆盖安装到 iPhone 主屏幕、当前主流电脑浏览器，以及目标 Redmi
Android 设备。

## 17. 实施阶段

开发按照用户端平台优先级推进。

### 阶段 1：核心 PWA

- 周期领域逻辑和契约测试；
- PocketBase 服务器和迁移；
- 用户名身份验证；
- 首页、分类、事项、完成历史和设置；
- IndexedDB 和离线操作队列；
- 同步和冲突处理；
- 响应式电脑布局；
- 可安装的 iPhone PWA。

### 阶段 2：通知

- 设备登记；
- 浏览器 Web Push；
- 每日摘要；
- 重要提醒；
- 免打扰时间；
- 通知去重和通知设置。

### 阶段 3：生产分发

- 加固后的 Docker 镜像；
- Compose 和中英文 1Panel 部署文档；
- 自动备份和升级路径；
- GitHub Actions；
- Docker Hub 多架构发布；
- 发布文档。

### 阶段 4：Android APK

- Capacitor 包装；
- 本地通知安排；
- 生命周期同步；
- 签名和 GitHub Release 发布；
- 在 Redmi 设备上测试安装和升级。

## 18. 版本 1 验收标准

满足以下条件时，版本 1 视为完成：

- iPhone PWA、电脑浏览器和 Android APK 使用同一账号和同一份数据；
- 客户端离线时可以查看和修改数据；
- 待同步修改上传后不会产生重复完成记录；
- 同字段并发编辑不会被静默丢失；
- 相对周期和固定日历周期通过共享的边界情况测试；
- 每日摘要和重要提醒遵守各设备设置与免打扰时间；
- 替换 Docker 容器后数据仍然存在；
- `amd64` 和 `arm64` 镜像已经发布到 Docker Hub；
- GitHub Release 已附带签名 APK；
- 升级会在迁移前创建可恢复备份；
- 容器只绑定回环地址并位于所有者管理的 1Panel 反向代理之后；
- 完整 JSON 导出能够恢复全部应用数据。

## 19. 明确延后的功能

以下内容不包含在版本 1 中：

- 公开注册；
- 多个活跃用户；
- 家庭共享；
- 邮件验证和邮件找回密码；
- 附件和照片；
- Google、Apple 或其他第三方登录；
- 应用商店分发；
- iOS 原生应用；
- 通用任务、习惯、日记和自由笔记；
- 公开个人主页；
- 分析统计或遥测；
- VPS 自动更新；
- 每天逐事项发送逾期通知；
- Android 通知依赖 Google Firebase。

这些限制让第一个版本能够专注于可靠的个人周期维护流程。
